div#createCounterModal(class="fixed inset-0 bg-black bg-opacity-50 z-40 flex items-center justify-center hidden")
    div(class="bg-[#1a1a1a] border border-[#313131] shadow-lg p-6 w-full max-w-md mx-4")
        h3(class="font-bold mb-4") Create New Counter
        
        label(class="block text-sm text-gray-400 mb-1" for="counterName") Counter Name
        input#counterName(class="w-full p-2 mb-4 bg-[#242424] border border-[#313131] text-white" type="text" name="name" placeholder="Enter counter name")
        
        div(class="flex items-center mb-6")
            span#privateLabel(class="text-sm text-gray-400 mr-2 font-bold") Private
            label(class="relative inline-block w-10 h-5")
                input#publicToggle(class="hidden" type="checkbox" name="public")
                span(class="absolute cursor-pointer inset-0 bg-[#313131] rounded-full transition-all duration-300")
                    span(class="absolute bg-white rounded-full w-3.5 h-3.5 left-1 top-[3px] transition-all duration-300")
            span#publicLabel(class="text-sm text-gray-400 ml-2") Public
        
        div(class="flex justify-end gap-3")
            button#cancelCounterBtn(class="px-2 rounded hover:bg-[#313131] transition-colors" type="button") Cancel
            button#createCounterBtn(class="bg-blue-600 text-white rounded hover:bg-blue-700 transition-colors" type="button") Create

div#shareCounterModal(class="fixed inset-0 bg-black bg-opacity-50 z-40 flex items-center justify-center hidden")
    div(class="bg-[#1a1a1a] border border-[#313131] shadow-lg p-6 w-full max-w-md mx-4")

        h3#shareModalTitle(class="font-bold mb-4") Share Counter
        
        div#shareModalContent(class="mb-4")
            div#loadingIndicator(class="text-center py-4") Generating share link...
            
            div#shareUrlContainer(class="bg-gray-700 p-3 rounded mb-4 break-all text-gray-200 border border-gray-600 hidden")

        div(class="flex justify-end gap-3")
            button#closeShareModalBtn(class="bg-red-600 hover:bg-red-700 text-white py-2 px-4 rounded w-full transition-colors") Cancel
            button#copyShareLinkBtn(class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded w-full transition-colors hidden") Copy Link